===== s16verify/mutate_s16.py =====
OK    沙箱不校验越界（去掉 relative_to）
OK    不展开 ~（与 shell 真实行为不一致）
OK    不豁免命令位置（带空格的绝对路径程序被误杀）
OK    不剔除 URL（Windows 上 Path('http://…').is_absolute() 为真 → 全判越界）
OK    不跟踪引号状态（词被空格切碎，带空格的程序路径掉进非命令位置）
OK    去掉「必须含分隔符」门槛（每个词都成候选 → `cd ..` 被误杀）
OK    有引擎时工具也二次否决（推翻引擎的 allow_once/allow_always）
OK    收紧判据被记忆短路（等价于挪到 `_always`/`_turn` 之后）
OK    收紧判据被 `commands.allow` 规则短路（掉进规则层，排在 allow 之后）
OK    DENY 档降成 ASK（相对逃逸从「硬拒」变成「可被允许」）
OK    ask 档判据去掉（绝对越界 / 含变量不再问）
OK    引擎层自己再实现一份沙箱语义（两份拷贝漂移）
OK    确认框不传 ctx（[沙箱] 出处静默消失）
OK    denial_hint 不再产出处（只剩一句「没权限」）
OK    loop 的 DENY 分支不传 ctx 给 describe（接线断一半）
OK    loop 的 ASK 分支不传 ctx 给 denial_hint（接线断一半）

16/16 个变异体被抓住
EXIT=0
===== evalverify/mutate_b1.py =====
OK    开围栏又只认裸围栏 / diff / patch（配对整体错位一格）
OK    兜底一路切到全文结尾（散文混进补丁）
OK    `_is_diff_block` 恒真（python 分析块也会被当成补丁）
OK    多块时取第一个而不是最长（示意块会顶掉真补丁）
OK    指纹少一项（pytest 版本 —— 决定 judge 结论的那一项没记）
OK    两项哈希算成同一个文件（配对断掉，字段俱全但指错东西）
OK    哈希截断长度改变（与文档/复核脚本约定的 12 位不一致）
OK    "量不到"写成空串（读起来像"文件是空的"，那是另一回事）

8/8 个变异体被抓住
EXIT=0
===== evalverify/mutate_b2.py =====
OK    判据退回 `rc == 0`（全 skip 时退出码也是 0 → 注入照样通过）
OK    判据退回 `rc == 0`（同一个机制，打在纯函数那一格上）
OK    把"数不出通过数"读成 0（真实通过会被翻成失败 —— 修 bug 制造新假阴性）
OK    从 6 行 summary 而不是完整 out 里解析计数（45a4d4b3 那一格会翻车）
OK    统计行丢掉 `(H:MM:SS)` 那一档（跑满 60 秒的判定静默失去 P4 保护）
OK    恢复挪到 after 快照**之前**（zero_change 变 True、篡改证据消失）
OK    恢复挪到 judge **之后**（篡改已经生效，恢复毫无意义）
OK    不记 judge_restored（等于「我们悄悄把现场清干净了」）
OK    guard 关着时用 `[]` 而不是 `None`（「没做」被读成「做了、没事」）
OK    恢复失败被吞掉（盘面不可信，却照跑 judge）
OK    丢掉相对路径那一档（`tests/__init__.py` 抓不到了）
OK    名字集合里加 `__init__.py`（**反向**变异：会把合法源码的修复还原回去）
OK    闸门的前置断言被删（金标准改了根 conftest.py 也照放行）
OK    判定期不再把 pytest 钉在工作区（`--rootdir`/`--confcutdir` 拿掉）
OK    把非 .py 的测试数据文件也当节点交给 pytest（`tests/files/*.sql` 会让它以退出码 4 收场，任务被误判成'判定无效'）
OK    默认值翻回去（guard 默认关）
OK    `--no-guard-judge` 被删（旧开关回来 → 归档口径复现不了）

17/17 个变异体被抓住
EXIT=0
===== evalverify/mutate_c.py =====
OK    仓名没登记时回落到 tinydb（安静地克隆错仓库，闸门照样出'有效任务'）

1/1 个变异体被抓住
EXIT=0
